{
  "name": "weave-steel-docs-sync",
  "version": "0.0.0",
  "description": "Deterministic run-first Weave docs sync example with domain-shaped outputs.",
  "private": true,
  "type": "module",
  "scripts": {
    "demo": "tsx src/index.ts",
    "webhook-demo": "tsx src/webhook-demo.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "weave": "file:../.."
  }
}
