# Optional model-backed assistant demo configuration.
# Copy this file to .env or export the variable in your shell.
OPENCODE_API_KEY=replace-with-your-opencode-api-key

# Optional server port for npm run assistant:server.
# PORT=3000
